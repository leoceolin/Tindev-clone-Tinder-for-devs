import React from 'react';
import ReactDOM from 'react-dom'; //para lidar com o browser

import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); //insere os dados de App para a div root e gera o html, executado uma única vez


